
{
    "version": "0.2.0",

    "configurations": [
        {
            "preLaunchTask": "Cargo Build",

            "name": "Debug Launch",
            "type": "lldb",

            "request": "launch",

            "program": "${workspaceRoot}/target/debug/riscv",
            "args": [ "./tests/add-addi.bin" ],

            "cwd": "${workspaceRoot}"
        }
    ]
}
